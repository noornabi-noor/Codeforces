#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        long long int N,ans;
        cin >> N;
        ans=floor(sqrt(N));
        cout << ans << endl;
    }

    return 0;
}
